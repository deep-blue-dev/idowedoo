<% if remotipart_submitted? %>
    CaseCreator.addImageFromURL("<%= escape_javascript(@image.image.url).gsub('&quot;', "'") %>")
<% else %>
    CaseCreator.addImageFromURL("<%= escape_javascript(@image.image.url) %>")
<% end %>




  